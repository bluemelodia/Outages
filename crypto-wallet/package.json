{
	"name": "crypto-wallet-frontend",
	"version": "1.0.0",
	"description": "Crypto wallet frontend",
	"main": "index.html",
	"scripts": {
		"start": "live-server frontend --open=index.html",
		"dev": "live-server frontend --open=index.html"
	},
	"devDependencies": {
		"live-server": "1.2.2",
		"local-cors-proxy": "1.1.0"
	},
	"engines": {
		"node": ">=16.0.0"
	},
	"dependencies": {
		"pino": "^9.12.0",
		"pino-logflare": "^0.5.2"
	}
}
